<Window x:Class="MusicCatallogApp.GUI.AdminWindow.BlockUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicCatallogApp.GUI.AdminWindow"
        mc:Ignorable="d"
        Title="Block User" Height="450" Width="700">
    <Grid Margin="10" Background="#FFDCA47C">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Block User" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>

        <DataGrid x:Name="dgUsers" Grid.Row="1" AutoGenerateColumns="True" IsReadOnly="True" Margin="0,10" />

        <Button x:Name="btnChoese" Content="Choose" Grid.Row="2" Width="100" Height="30" HorizontalAlignment="Center" VerticalAlignment="Top" FontWeight="Bold" Margin="0,10" Click="btnChoese_Click" Background="#FF698474" />
    </Grid>
</Window>
